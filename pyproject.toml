[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="textscope"
version="0.1.0"
description="Dependency-free text inspection helpers"
requires-python=">=3.10"
[tool.setuptools.packages.find]
where=["src"]
